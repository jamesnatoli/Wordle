This is a repository for my version of the NYT game Wordle

To play, run **python test.py**

To use different length words, use **python test.py -l 8** for example

You will need to have **termcolor** installed, which can be done via **pip install termcolor**